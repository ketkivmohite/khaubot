# ── FastAPI core ──────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1

# ── Database ──────────────────────────────────
sqlmodel==0.0.18
psycopg2-binary==2.9.9
pgvector==0.2.5
alembic==1.13.1              # DB migrations

# ── NLP ───────────────────────────────────────
sentence-transformers==2.7.0
langdetect==1.0.9            # detect Hindi/Marathi/English
indic-nlp-library==0.91      # IndicNLP for Indian languages

# ── API utilities ─────────────────────────────
httpx==0.27.0                # async HTTP (Django → FastAPI calls)
pydantic[email]==2.7.1
python-multipart==0.0.9      # file uploads (vendor photos)
