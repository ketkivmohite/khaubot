services:
  - type: web
    name: khaubot-backend
    env: python
    rootDir: khaubot/backend
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
